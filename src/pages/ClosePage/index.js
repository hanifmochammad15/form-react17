import { React } from 'react';
import {  withRouter  } from 'react-router-dom';

const ClosePage = (props) => {
  return (
      <div>
        <h1>CLOSE PAGE</h1>
      </div>
    );
}

export default withRouter(ClosePage);
